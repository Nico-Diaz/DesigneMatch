<section class="article-carousel-section">
  <div class="pre-carousel">
    <p class="intro-text">
      DesignMatch es una plataforma digital colaborativa con sede en Mendoza, Argentina, 
      que conecta a pymes, emprendedores y profesionales del diseño. Nuestro objetivo es 
      potenciar el talento creativo, generar vínculos confiables y aportar soluciones 
      innovadoras que transformen cada proyecto en una experiencia única y profesional.
    </p>

    <h2 class="cta-heading">¿Conectamos para diseñar tu idea?</h2>
  </div>
  </section>
  
  <style>
    /* --- SECCIÓN 2 (TEXTO INTRO) --- */
    .article-carousel-section {
      min-height: 20vh; /* reduced so it doesn't force a full viewport gap */
      padding: 20px 0; /* reduced top padding so Intro sits closer to previous section */
      color: white;
      position: relative;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .intro-text {
      max-width: 900px;
      margin: 0 auto 24px;
      text-align: center;
      font-size: 1.5rem;
      line-height: 1.6;
      padding: 0 20px;
      opacity: 0.95;
    }

    .cta-heading {
      text-align: center;
      font-size: 2.5rem;
      font-weight: 600;
      margin: 0 auto 10px;
      max-width: 800px;
      padding: 0 10px;
      background: linear-gradient(135deg, #fff 30%, #ffe4d6);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }

</style>
<section class="hero-section">
  <div class="card">
    <header>
      <span class="brand">DesignMatch</span>
      <nav>
        <a href="#" class="active">Home</a>
        <a href="#team-title">Sobre nosotros</a>
        <a href="https://www.canva.com/folder/FAF38ScWG8M" target="_blank">Proyectos</a>
        <a href="#contact-section">Contactos</a>
      </nav>
    </header>
    <main>
      <div class="main-content">
        <img src="public/ChatGPT Image 3 oct 2025, 09_26_45-Photoroom.png" alt="Logo DesignMatch" class="logo">
        <p class="slogan">Encontrá el<br> diseñador ideal<br> para tu proyecto.</p>
      </div>
    </main>
    <footer>
      <span>www.designmatch.com.ar</span>
      <span>desingmatch@gmail.com</span>
    </footer>
  </div>
</section>
<style>

/* --- SECCIÓN 1 (HERO) --- */

.hero-section {
    min-height: 100vh; /* reduced from 100vh so next section appears sooner */
    height: auto;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 20px;
    box-sizing: border-box;
}



.card {
    width: 90vw;
    height: auto; /* let content dictate height */
    min-height: 65vh; /* keep a strong hero presence but smaller */
    max-height: 85vh;
    background: rgba(255, 255, 255, 0.64);
    border-radius: 25px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    box-sizing: border-box;
    backdrop-filter: blur(12px);
    border: 2px solid rgba(255, 255, 255, 0.3);
    box-shadow: 0 0 30px rgba(0, 0, 0, 0.15);
}


header {
    display: flex;
    justify-content: space-between;  
    align-items: center;
    padding: 20px 40px;
    width: 100%;
    box-sizing: border-box;
}


.brand {
    font-weight: 600;
    color: #d85f3a;
    font-size: 1.2rem;
    white-space: nowrap;
}



nav {
    display: flex;
    align-items: center;
    gap: 20px;
    margin-left: 20px;

}

nav a {
    text-decoration: none;
    width: 100%;
    font-weight: 500;
    color: transparent;
    background: radial-gradient(circle at 50% 50%, #ff3131, #ff914d);
    -webkit-background-clip: text;
    background-clip: text;
    opacity: 0.8;
    transition: opacity 0.3s;
    white-space: nowrap;
    font-size: 0.95rem;
}

nav a.active { font-weight: 700; opacity: 1; }
nav a:hover { opacity: 1; text-shadow: 5px 5px 5px radial-gradient(circle at 50% 50%, #ff3131, #ff914d); font-size: 1rem; }
main {
    flex: 1;
    display: flex;
    justify-content: flex-start;
    align-items: center;
    min-height: 320px; /* slightly reduced */
    margin: 40px 0;
    padding: 0 60px;
}

.main-content {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    width: 100%;
    max-width: 1400px;
    position: relative;
}
.logo {
    height: auto;
    width: 60%;
    max-width: 60%;
    filter: brightness(100%) contrast(150%);
    object-fit: contain;
    margin-right: auto;
    transform: translateX(-40px);
}
.slogan {
    font-size: 70px;
    font-weight: 700;
    text-align: right;
    color: transparent;
    background: radial-gradient(circle at 50% 50%, #ff3131, #ff914d);
    -webkit-background-clip: text;
    background-clip: text;
    line-height: 1.3;
    max-width: 800px;
}

footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 14px;
    font-weight: 500;
    color: transparent;
    background: radial-gradient(circle at 50% 50%, #ff3131, #ff914d);
    -webkit-background-clip: text;
    background-clip: text;
    border-top: 1px solid rgba(255, 255, 255, 0.3);
    padding: 15px 40px;
    width: 100%;
    box-sizing: border-box;
    margin-top: auto;
}

/* RESPONSIVO MÓVIL (Solo lo relevante al Hero) */
@media (max-width: 440px) {
    .card {
        width: 95vw;
        height: 90vh;
        padding: 18px;
        gap: 18px;
    }

    .card header {
        flex-direction: row;
        justify-content: center;
        gap: 10px;
        padding: 0;
    }
    .card footer {
        flex-direction: column;
        gap: 10px;
    }
    header { padding: 12px 16px; }
    .brand, .logo { display: none; } /* Ocultar en móvil */
   
    nav {
        gap: 12px;
        margin: 0;
        justify-content: center;
    }
    nav a { font-size: 80%; padding: 6px 3px; }

    main {
        padding: 0 8px;
        gap: 18px;
        justify-content: center;
    }
    .main-content { padding: 0; gap: 12px; }
   
    .slogan {
        font-size: 2.4rem;
        text-align: center;
        width: 100%;
    }
}
</style>